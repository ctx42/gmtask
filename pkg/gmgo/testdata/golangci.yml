# Minimal golangci-lint config used only by tests. It enables just enough to
# exercise a failing lint (staticcheck's SA5009) while leaving clean code
# issue-free. It is not representative of any real project configuration.
version: "2"
linters:
  default: none
  enable:
    - staticcheck
